{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least-privilege capability for the main window. M0 grants only core defaults — no plugin commands are exposed to the webview yet (deny by default).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
