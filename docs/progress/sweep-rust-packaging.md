1. **MAJOR** â€” `apps/ui/src-tauri/tauri.conf.json:59-61` + `packaging/README.md:144-145,204` â€” updater endpoint `https://github.com/AlexKapadia/omni/releases/latest/download/latest.json` returns **404 today (verified live)** and points at the `upstream` remote (`AlexKapadia/omni`) while the working repo's `origin` is `bhaskaraanjana/omni`, so shipped apps will never receive an update (and every release-build launch emits a spurious `updater:error`). Fix: point the endpoint (and the README's `gh secret set --repo`) at the repo that will actually publish releases, and only ship once `latest.json` resolves.

2. **REFUTED (suspect a, crash/blocking part)** â€” `updater_launch_check.rs:55-83` spawns the check on the async runtime, maps every failure (dead endpoint, offline, missing config) to an `updater:error` event, and is release-only (`lib.rs:118-120`); startup is never blocked or crashed. Pubkey is present and `createUpdaterArtifacts: true` is set (`tauri.conf.json:35,58`).

3. **MAJOR** â€” `apps/ui/src-tauri/src/meeting_toast_window.rs:41-50` and `apps/ui/src-tauri/src/captions_overlay_window.rs:43-52` â€” both compute **logical** coordinates (physical size Ã· scale) but pass them to `PhysicalPosition`, so on any DPI scale â‰  100% the toast/captions render far from the bottom-right/bottom-center (e.g. at 150% on 2560Ã—1440 the toast sits ~700 px off both edges); they also ignore `monitor.position()`, so on multi-monitor setups where the current monitor isn't at (0,0) the window lands on the wrong monitor. Fix: copy the correct math already used in `dictation_pill_window.rs:244-257` (multiply logical constants by scale, add `monitor.position()` offset).

4. **MAJOR** â€” `apps/ui/src-tauri/src/lib.rs:40-50` (`reveal_path_in_explorer`) â€” the webview-supplied string is passed unvalidated to `explorer.exe <path>`; explorer opens files by association, so a path to any `.exe`/`.bat`/etc. **executes it**, and the command is invocable from every webview (Tauri app commands aren't ACL-gated), making it an arbitrary-program-launch primitive if any webview is ever compromised. Fix: canonicalize, require the path to exist and be a directory (or use `explorer /select,` for files), and reject otherwise.

5. **MAJOR** â€” `apps/ui/src-tauri/src/engine_sidecar.rs:109-163` â€” no port-conflict handling anywhere: if 127.0.0.1:8765 is held (e.g. by an orphaned engine from a previous crashed shell), the fresh engine exits at bind, and the supervisor respawns it forever (30 s backoff cap) with **only a log line â€” no event/state ever reaches the UI**, so the user sees a permanently dead app with no explanation. Fix: detect repeated fast exits (N consecutive sub-BACKOFF_RESET_AFTER runs) and emit a user-visible `engine:unhealthy` event; probe/free the port (or pass an alternate port) before respawn.

6. **MAJOR** â€” `apps/ui/src-tauri/src/engine_sidecar.rs:221-232` â€” the child tree is killed only via `taskkill` on the cooperative `shutdown()`/`RunEvent::Exit` path (`lib.rs:143-153`); if the shell is force-terminated (Task Manager, crash, `SIGKILL`-equivalent) no exit event fires and the engine (plus its `uv`-wrapped python in dev) is orphaned, keeping port 8765 and potentially the mic â€” violating the module's own "no orphaned engine" contract (lines 6-8). Fix: put the child in a Win32 Job Object with `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE` so OS teardown of the shell kills the engine unconditionally.

7. **MAJOR** â€” `pyproject.toml:49-56` vs `engine/stt/openai_compatible_stt.py:10` (also lazy uses in `engine/wiring/provider_key_live_validation.py:116`, `engine/microsoft/graph_session.py:85`, `engine/google/google_session.py:123,154`, `engine/google/oauth_desktop_flow.py:140`) â€” `httpx` is imported by runtime engine code but is declared only in the **dev** dependency group; the PyInstaller bundle currently gets it only because `uv sync` (README:116, release.yml:50) installs the dev group by accident, and any `--no-dev`/wheel install crashes on `import httpx` (BYOK STT, key validation, Google/Microsoft flows). Fix: move `httpx` into `[project].dependencies`.

8. **MINOR** â€” `apps/ui/src-tauri/Cargo.toml:27-29` â€” comment claims `tauri-plugin-updater` is "deliberately NOT registered in the builder yet", but `lib.rs:82` registers it and `lib.rs:94-95` exposes its commands; **suspect (d) CONFIRMED** as pure doc drift. Fix: delete/update the stale comment.

9. **REFUTED (suspect e)** â€” `tauri.conf.json:29` bundle targets `dmg/app/deb/appimage`: tauri-bundler's `package_types()` filters the explicit list to host-native types (verified in `tauri-bundler/src/bundle/settings.rs`), so on Windows only NSIS+MSI build â€” silent no-op, not a failure â€” and the non-Windows targets are genuinely used by the CI matrix (`release.yml` per `packaging/README.md:181-186`).

10. **REFUTED (suspect f)** â€” `lib.rs:103-141` setup: tray, updater check, pill, captions, toast, and sidecar are each individually guarded (`if let Err` + `warn!` / internal never-fail contracts); the only fatal step is the documented context build `.expect` (line 140-141); nothing in setup blocks (sidecar work is on its own thread, updater check on the async runtime).

11. **REFUTED (suspect b, commands + capability part)** â€” the 4 toast commands (`meeting_toast_window.rs:62-112`) take only `bool`/`Option<String>` with no panicking paths (all fallible calls are `?` or `let _`), and `capabilities/meeting-toast.json` grants only `core:default` + window show/hide/set-focus â€” least privilege holds; no fs/shell/http anywhere in `capabilities/*.json` (default/pill/captions/onboarding are `core:default` Â± `allow-hide` Â± `dialog:allow-open`).

12. **REFUTED (suspect g, unwrap/panic part)** â€” sweep of all 10 registered handlers found no user-reachable `unwrap`/panic: `inject_dictation_text` runs its blocking Win32 work in `spawn_blocking` and converts even the join error into an honest failure payload (`dictation_text_injection.rs:83-93`); `set_dictation_hotkey` is total via `accelerator_from_keys` fallback (`dictation_hotkey_accelerator.rs:58-68`). Item 4 above is the sole command-input finding.

13. **REFUTED (suspect i, hidden-imports drift)** â€” `packaging/omni-engine.spec:45` uses `collect_submodules("engine")` generically, so the post-2026-07-09 modules (`engine/detect/meeting_process_watcher.py`, `engine/protocol/`, new `stt/`/`wiring/` files) are auto-included, and their third-party imports are traced by Analysis; migrations ship at both candidate paths (spec:58-61); heavy/optional stacks (`torch`, `nemo`, `pywhispercpp`) are excluded-or-absent and only lazily imported behind fail-closed guards â€” the one real packaging break is the `httpx` declaration issue in item 7.

14. **MINOR** â€” `pyproject.toml:55` â€” dev dependency `httpx2>=2.5.0` looks like a typosquat/accidental package alongside real `httpx` and nothing in the repo imports `httpx2`; a look-alike name in the dev sync is a supply-chain risk. Fix: remove it (or document why it exists) and re-lock.

15. **MINOR** â€” `apps/ui/src-tauri/src/meeting_toast_window.rs:42` / `captions_overlay_window.rs:44` â€” positioning queries `current_monitor()` of a hidden, never-shown window, which resolves to the window's default (primary-ish) monitor rather than the monitor the user is actually working on, so the toast can appear on the wrong screen even after item 3 is fixed. Fix: position relative to the monitor containing the foreground/main window (or `primary_monitor()` explicitly, matching the doc comment's stated intent).
