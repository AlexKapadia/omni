"""Test package for the Omni engine.

Required as a real package on this repo: without ``tests/__init__.py``,
Python can resolve ``import tests...`` to an unrelated site-packages
package and collection fails (see claude.md §7.3).
"""
